Clears a
```
for (i = 0, i < N; i++);
  a[i] = 0;
```

Reads date into a
```
for (i = 0, i < N; i++);
  scanf("%d", &a[i]);
```

Sums the elements of a
```
for (i = 0, i < N; i++);
  sum += a[i];
```
