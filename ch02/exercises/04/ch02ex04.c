// Computes the dimensional weight of a 12'' x 10'' x 8'' box

#include <stdio.h>

int main(void)
{
	int i, j, k;
	float x, y;

	printf("i : %d\nj : %d\nk : %d\n", i, j, k);
	printf("x : %f\ny : %f\n", x, y);

	return 0;
}